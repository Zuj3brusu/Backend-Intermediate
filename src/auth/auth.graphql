type Mutation {
  login(email: String!, password: String!): AuthPayload!
}

type Query {
  profile : AuthPayload!
}

schema {
  query: Query
}

type AuthPayload {
    firstName: String!
    lastName: String!
}