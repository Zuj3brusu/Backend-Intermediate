type Query {
  getUsers : [User]
  getUserByEmail(email: String) : User
}

type Mutation {
  createUser(
    firstName: String,
    lastName: String,
    email: String,
    password:String,
    ): User
}

type User {
  firstName: String
  lastName: String
  email : String
  password : String
}