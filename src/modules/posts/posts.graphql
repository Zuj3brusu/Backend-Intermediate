type Query {
  getPosts : [Post]
  getPostsByUserKey(email: String) : [Post]
}

type Mutation {
  createPost(
    postContent: String,
    ): Post
}

type Post {
  postContent: String
  creatorEmail: String
}